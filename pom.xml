<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/>
    </parent>

    <groupId>io.oryxos</groupId>
    <artifactId>oryxos</artifactId>
    <version>0.1.5-RELEASE</version>
    <packaging>pom</packaging>

    <name>OryxOS</name>
    <description>Enterprise Agent OS</description>

    <properties>
        <java.version>21</java.version>
        <spring-ai.version>1.1.8</spring-ai.version>
        <picocli.version>4.7.6</picocli.version>
        <sqlite-jdbc.version>3.53.2.1</sqlite-jdbc.version>
        <!-- 025：覆盖 Boot BOM 的 42.7.11——CVE-2026-54291（CVSS 8.2）修复于 42.7.12（OSV 实证），OWASP 门禁线 8.0 -->
        <postgresql.version>42.7.12</postgresql.version>
        <pdfbox.version>3.0.8</pdfbox.version>
        <oapi-sdk.version>2.8.5</oapi-sdk.version>

        <!-- Infrastructure libraries -->
        <jackson-bom.version>2.21.5</jackson-bom.version>
        <httpclient5.version>5.6.4</httpclient5.version>
        <httpcore5.version>5.4.3</httpcore5.version>
        <log4j2.version>2.25.5</log4j2.version>
        <tomcat.version>10.1.59</tomcat.version>
        <!-- CVE-2025-48924: ClassUtils.getClass StackOverflowError before 3.18.0 -->
        <commons-lang3.version>3.18.0</commons-lang3.version>
        <logstash-logback.version>7.4</logstash-logback.version>
        <!-- Boot 3.5.x line; 2.9+/3.x need Boot 4 -->
        <springdoc.version>2.8.17</springdoc.version>
        <!-- springdoc 2.8.17 still ships older swagger-ui; override for DOMPurify CVEs -->
        <swagger-ui.version>5.32.13</swagger-ui.version>

        <!-- Quality / format / security plugins -->
        <spotless.version>2.43.0</spotless.version>
        <maven-pmd-plugin.version>3.21.2</maven-pmd-plugin.version>
        <pmd.version>6.55.0</pmd.version>
        <p3c.version>2.1.1</p3c.version>
        <maven-checkstyle-plugin.version>3.5.0</maven-checkstyle-plugin.version>
        <checkstyle.version>10.18.2</checkstyle.version>
        <spotbugs-maven-plugin.version>4.8.6.4</spotbugs-maven-plugin.version>
        <spotbugs.version>4.8.6</spotbugs.version>
        <findsecbugs.version>1.13.0</findsecbugs.version>
        <dependency-check.version>12.2.2</dependency-check.version>
        <nvd.datafeed.url>https://dependency-check.github.io/DependencyCheck_Builder/nvd_cache/</nvd.datafeed.url>

        <!-- OWASP Dependency-Check is skipped by default for fast local builds and enabled in CI. -->
        <owasp.skip>true</owasp.skip>
        <!-- Integration smoke tests stay opt-in but can be enabled with -DexcludedGroups=. -->
        <excludedGroups>integration</excludedGroups>
    </properties>

    <modules>
        <module>oryxos-core</module>
        <module>oryxos-persona</module>
        <module>oryxos-provider</module>
        <module>oryxos-memory</module>
        <module>oryxos-knowledge</module>
        <module>oryxos-tool</module>
        <module>oryxos-channel-cli</module>
        <module>oryxos-channel-feishu</module>
        <module>oryxos-channel-wecom</module>
        <module>oryxos-channel-dingtalk</module>
        <module>oryxos-channel-slack</module>
        <module>oryxos-channel-discord</module>
        <module>oryxos-channel-telegram</module>
        <module>oryxos-channel-whatsapp</module>
        <module>oryxos-channel-teams</module>
        <module>oryxos-channel-gchat</module>
        <module>oryxos-channel-mattermost</module>
        <module>oryxos-channel-matrix</module>
        <module>oryxos-channel-qq</module>
        <module>oryxos-channel-douyin</module>
        <module>oryxos-channel-weixin</module>
        <module>oryxos-channel-weixin-kf</module>
        <module>oryxos-channel-weixin-mp</module>
        <module>oryxos-channel-weixin-mini</module>
        <module>oryxos-channel-alipay</module>
        <module>oryxos-web</module>
        <module>oryxos-storage</module>
        <module>oryxos-cli</module>
        <module>oryxos-boot</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <!-- Spring AI BOM -->
            <dependency>
                <groupId>org.springframework.ai</groupId>
                <artifactId>spring-ai-bom</artifactId>
                <version>${spring-ai.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- 039：OpenTelemetry BOM——trace 导出（SpanRecorder 的装配层实现），版本统一由 BOM 管理 -->
            <dependency>
                <groupId>io.opentelemetry</groupId>
                <artifactId>opentelemetry-bom</artifactId>
                <version>1.44.1</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- 039：OTLP exporter 经 okhttp 传递带入 kotlin-stdlib 1.9.25——CVE-2026-53914（9.8，
                 <2.4.20 反序列化）OWASP 门禁阻断，钉到修复版（okhttp 4.x 与 kotlin-stdlib 2.x 二进制兼容） -->
            <dependency>
                <groupId>org.jetbrains.kotlin</groupId>
                <artifactId>kotlin-stdlib</artifactId>
                <version>2.4.20</version>
            </dependency>
            <dependency>
                <groupId>org.jetbrains.kotlin</groupId>
                <artifactId>kotlin-stdlib-jdk8</artifactId>
                <version>2.4.20</version>
            </dependency>
            <dependency>
                <groupId>org.jetbrains.kotlin</groupId>
                <artifactId>kotlin-stdlib-jdk7</artifactId>
                <version>2.4.20</version>
            </dependency>
            <!-- kotlin-stdlib-common 2.x 起只发 pom 无 jar（classes 并入主件），不可钉版本；
                 升 okio 到 3.9+（其 okio-jvm 直依 kotlin-stdlib，不再传递引入 common）断根 -->
            <dependency>
                <groupId>com.squareup.okio</groupId>
                <artifactId>okio</artifactId>
                <version>3.9.1</version>
            </dependency>

            <!-- Internal modules -->
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-core</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-persona</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-provider</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-memory</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-knowledge</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-tool</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-channel-cli</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-channel-feishu</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-channel-wecom</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-channel-dingtalk</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-channel-slack</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-channel-discord</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-channel-telegram</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-channel-whatsapp</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-channel-teams</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-channel-gchat</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-channel-mattermost</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-channel-matrix</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-channel-qq</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-channel-douyin</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-channel-weixin</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-channel-weixin-kf</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-channel-weixin-mp</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-channel-weixin-mini</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-channel-alipay</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-web</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-storage</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>io.oryxos</groupId>
                <artifactId>oryxos-cli</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- Apache PDFBox：文本型 PDF 解析（014 知识库，唯一新增运行时依赖） -->
            <dependency>
                <groupId>org.apache.pdfbox</groupId>
                <artifactId>pdfbox</artifactId>
                <version>${pdfbox.version}</version>
            </dependency>

            <!-- Picocli -->
            <dependency>
                <groupId>info.picocli</groupId>
                <artifactId>picocli</artifactId>
                <version>${picocli.version}</version>
            </dependency>

            <!-- SQLite JDBC -->
            <dependency>
                <groupId>org.xerial</groupId>
                <artifactId>sqlite-jdbc</artifactId>
                <version>${sqlite-jdbc.version}</version>
            </dependency>

            <!-- SpotBugs 局部抑制注解（带 justification 的显式声明，provided 不进运行时） -->
            <dependency>
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-annotations</artifactId>
                <version>4.8.6</version>
                <scope>provided</scope>
            </dependency>

            <!-- Structured logging: JSON encoder for Logback -->
            <dependency>
                <groupId>net.logstash.logback</groupId>
                <artifactId>logstash-logback-encoder</artifactId>
                <version>${logstash-logback.version}</version>
            </dependency>

            <!-- OpenAPI / Swagger UI -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>
            <dependency>
                <groupId>org.webjars</groupId>
                <artifactId>swagger-ui</artifactId>
                <version>${swagger-ui.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <!-- #453：构建环境硬约束（fail）+ 依赖收敛软检查（仅告警，存量收敛问题不阻断，
                     待清零后再升 fail——避免一次性引爆）。 -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-enforcer-plugin</artifactId>
                    <version>3.5.0</version>
                    <executions>
                        <execution>
                            <id>enforce-environment</id>
                            <goals><goal>enforce</goal></goals>
                            <configuration>
                                <rules>
                                    <requireJavaVersion><version>[21,)</version></requireJavaVersion>
                                    <requireMavenVersion><version>[3.8,)</version></requireMavenVersion>
                                </rules>
                            </configuration>
                        </execution>
                        <execution>
                            <id>warn-dependency-convergence</id>
                            <goals><goal>enforce</goal></goals>
                            <configuration>
                                <rules>
                                    <dependencyConvergence/>
                                </rules>
                                <fail>false</fail>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>

                <!-- Tests: exclude @Tag("integration") by default (CI skips smoke ITs);
                     run locally with: mvn test -Dgroups=integration -DexcludedGroups= -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <configuration>
                        <excludedGroups>${excludedGroups}</excludedGroups>
                    </configuration>
                </plugin>

                <!-- Format: Spotless + google-java-format -->
                <plugin>
                    <groupId>com.diffplug.spotless</groupId>
                    <artifactId>spotless-maven-plugin</artifactId>
                    <version>${spotless.version}</version>
                    <configuration>
                        <java>
                            <googleJavaFormat>
                                <style>GOOGLE</style>
                            </googleJavaFormat>
                            <removeUnusedImports/>
                            <importOrder/>
                            <trimTrailingWhitespace/>
                            <endWithNewline/>
                        </java>
                    </configuration>
                    <executions>
                        <execution>
                            <!-- Fast-fail: run the format check before compile/test
                                 (spotless:check defaults to the verify phase). -->
                            <phase>validate</phase>
                            <goals>
                                <goal>check</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <!-- Coding rules: Alibaba P3C on top of PMD (PMD 6.x for P3C compatibility) -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-pmd-plugin</artifactId>
                    <version>${maven-pmd-plugin.version}</version>
                    <configuration>
                        <!-- PMD 6.55 (newest that P3C 2.1.1 supports) tops out at Java 19,
                             so parse at the 17 grammar level. Code must avoid Java 18-21
                             *syntax* (record patterns, pattern switch); records/sealed are fine. -->
                        <targetJdk>17</targetJdk>
                        <printFailingErrors>true</printFailingErrors>
                        <skipPmdError>false</skipPmdError>
                        <linkXRef>false</linkXRef>
                        <analysisCache>true</analysisCache>
                        <rulesets>
                            <ruleset>rulesets/java/ali-naming.xml</ruleset>
                            <ruleset>rulesets/java/ali-oop.xml</ruleset>
                            <ruleset>rulesets/java/ali-concurrent.xml</ruleset>
                            <ruleset>rulesets/java/ali-exception.xml</ruleset>
                            <ruleset>rulesets/java/ali-constant.xml</ruleset>
                            <ruleset>rulesets/java/ali-flowcontrol.xml</ruleset>
                            <ruleset>rulesets/java/ali-set.xml</ruleset>
                            <ruleset>rulesets/java/ali-other.xml</ruleset>
                        </rulesets>
                    </configuration>
                    <dependencies>
                        <!-- Upgrade ASM ahead of PMD 6.55's bundled copy: the old one throws
                             "Unsupported class file major version 65" when type resolution reads
                             Java 21 bytecode on the auxclasspath. ASM 9.7 understands class v65. -->
                        <dependency>
                            <groupId>org.ow2.asm</groupId>
                            <artifactId>asm</artifactId>
                            <version>9.7</version>
                        </dependency>
                        <dependency>
                            <groupId>net.sourceforge.pmd</groupId>
                            <artifactId>pmd-core</artifactId>
                            <version>${pmd.version}</version>
                        </dependency>
                        <dependency>
                            <groupId>net.sourceforge.pmd</groupId>
                            <artifactId>pmd-java</artifactId>
                            <version>${pmd.version}</version>
                        </dependency>
                        <dependency>
                            <groupId>com.alibaba.p3c</groupId>
                            <artifactId>p3c-pmd</artifactId>
                            <version>${p3c.version}</version>
                        </dependency>
                    </dependencies>
                    <executions>
                        <execution>
                            <goals>
                                <goal>check</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <!-- Fallback style gate: Checkstyle (google_checks), errors only -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-checkstyle-plugin</artifactId>
                    <version>${maven-checkstyle-plugin.version}</version>
                    <configuration>
                        <configLocation>google_checks.xml</configLocation>
                        <violationSeverity>error</violationSeverity>
                        <failOnViolation>true</failOnViolation>
                        <includeTestSourceDirectory>true</includeTestSourceDirectory>
                    </configuration>
                    <dependencies>
                        <dependency>
                            <groupId>com.puppycrawl.tools</groupId>
                            <artifactId>checkstyle</artifactId>
                            <version>${checkstyle.version}</version>
                        </dependency>
                    </dependencies>
                    <executions>
                        <execution>
                            <id>checkstyle-validate</id>
                            <phase>validate</phase>
                            <goals>
                                <goal>check</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <!-- Security: SpotBugs + Find Security Bugs -->
                <plugin>
                    <groupId>com.github.spotbugs</groupId>
                    <artifactId>spotbugs-maven-plugin</artifactId>
                    <version>${spotbugs-maven-plugin.version}</version>
                    <configuration>
                        <effort>Max</effort>
                        <threshold>Low</threshold>
                        <plugins>
                            <plugin>
                                <groupId>com.h3xstream.findsecbugs</groupId>
                                <artifactId>findsecbugs-plugin</artifactId>
                                <version>${findsecbugs.version}</version>
                            </plugin>
                        </plugins>
                    </configuration>
                    <dependencies>
                        <dependency>
                            <groupId>com.github.spotbugs</groupId>
                            <artifactId>spotbugs</artifactId>
                            <version>${spotbugs.version}</version>
                        </dependency>
                    </dependencies>
                    <executions>
                        <execution>
                            <goals>
                                <goal>check</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <!-- Security: OWASP Dependency-Check (CVE scan of third-party deps) -->
                <plugin>
                    <groupId>org.owasp</groupId>
                    <artifactId>dependency-check-maven</artifactId>
                    <version>${dependency-check.version}</version>
                    <configuration>
                        <skip>${owasp.skip}</skip>
                        <failBuildOnCVSS>8</failBuildOnCVSS>
                        <!-- Use Dependency-Check's nightly NVD cache so fork PRs do not require an NVD API key. -->
                        <nvdDatafeedUrl>${nvd.datafeed.url}</nvdDatafeedUrl>
                        <!-- Fork PRs do not receive OSS Index credentials; NVD and CISA remain enabled. -->
                        <ossIndexAnalyzerEnabled>false</ossIndexAnalyzerEnabled>
                        <suppressionFile>${maven.multiModuleProjectDirectory}/config/dependency-check-suppressions.xml</suppressionFile>
                        <formats>
                            <format>HTML</format>
                            <format>JSON</format>
                        </formats>
                    </configuration>
                    <executions>
                        <execution>
                            <goals>
                                <goal>check</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-pmd-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>com.github.spotbugs</groupId>
                <artifactId>spotbugs-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.owasp</groupId>
                <artifactId>dependency-check-maven</artifactId>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <repository>
            <id>spring-milestones</id>
            <name>Spring Milestones</name>
            <url>https://repo.spring.io/milestone</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>spring-milestones</id>
            <name>Spring Milestones</name>
            <url>https://repo.spring.io/milestone</url>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </pluginRepository>
    </pluginRepositories>

</project>
