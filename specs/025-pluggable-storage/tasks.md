# Tasks: 可插拔存储（Pluggable Storage）

**Input**: Design documents from `/specs/025-pluggable-storage/`
**Prerequisites**: plan.md、research.md（R1~R9）、data-model.md、contracts/storage-migration.md、quickstart.md

**组织说明**: Flyway 迁移骨架是三个故事共同的地基，归 Foundational；US2（单机零破坏）先于 US1（PG 部署）实施——US2 不依赖 PG 面，且它守住的是存量用户。测试为迁移/契约类特性所必需（spec SC-005 明确要求两库全绿），故含测试任务。

## Phase 1: Setup

- [X] T001 oryxos-storage/pom.xml 加依赖：`flyway-core`、`flyway-database-postgresql`、`org.postgresql:postgresql`（均 Boot BOM 管版本，main 作用域打进 fat jar）、`io.zonky.test:embedded-postgres:2.0.7`（test）；确认 `mvn -q dependency:resolve -pl oryxos-storage` 走通（阿里云镜像慢，已预热 ~/.m2）
- [X] T002 oryxos-boot/src/main/resources/application.yml：删除 `spring.sql.init` 段；新增 `spring.flyway`（`locations: classpath:db/migration/{vendor}`、`baseline-on-migrate: true`、`baseline-version: 0`）

## Phase 2: Foundational（Flyway 迁移骨架——阻塞所有故事）

- [X] T003 新建 oryxos-storage/src/main/resources/db/migration/sqlite/V1__baseline.sql：schema.sql 原样平移（18 张 CREATE TABLE IF NOT EXISTS + 12 索引；`llm_pricing` 与 AuditSchemaUpgrade.java:106 的重复 DDL 去重为一份，保留 schema.sql:20-21 的索引分工注释语义——后加列索引归 V2）；删除 oryxos-storage/src/main/resources/schema.sql
- [X] T004 [P] 新建 oryxos-storage/src/main/java/io/oryxos/storage/migration/V2__AuditColumns.java（JavaMigration）：AuditSchemaUpgrade 逻辑平移——llm_calls 补 cost_micros/profile_name、tool_invocations 补 profile_name/blocked_by、三表补 trace_id、profile/trace 索引；幂等依据 PRAGMA table_info 照旧；checksum 返回固定值
- [X] T005 [P] 新建 oryxos-storage/src/main/java/io/oryxos/storage/migration/V3__MemoryAgentColumn.java：MemorySchemaUpgrade 平移（agent_name 补列 + idx_memory_agent）
- [X] T006 [P] 新建 oryxos-storage/src/main/java/io/oryxos/storage/migration/V4__ScheduleIdentity.java：ScheduleSchemaUpgrade 平移——三分支状态机（current/pre-retirement/legacy 整表重建含 UUID 键映射与行数对账）完整保留，结构无法识别抛异常拒启；注意 Flyway JavaMigration 的 Context 事务语义与原 BEGIN IMMEDIATE 手写事务的对齐
- [X] T007 [P] 新建 oryxos-storage/src/main/java/io/oryxos/storage/migration/V5__NotifyChannelConfig.java：NotifyChannelSchemaMigration 平移（config 补列）
- [X] T008 oryxos-cli/src/main/java/io/oryxos/cli/OryxOsRuntime.java 装配改造：注册 V2~V5 为 JavaMigration beans（仅 datasource url 为 jdbc:sqlite 时——PG 无存量库）；4 个升级类 Bean 定义与方法体内 upgrade() 调用全部移除；`@DependsOn("dataSourceScriptDatabaseInitializer")` 全部改为 Flyway 初始化 Bean；SecretMigration Bean 改挂 Flyway 之后（形参依赖链 providerRegistry/notifyChannelRegistry 不动）
- [X] T009 删除 oryxos-storage 四个升级类：AuditSchemaUpgrade.java、MemorySchemaUpgrade.java、ScheduleSchemaUpgrade.java、NotifyChannelSchemaMigration.java；SecretMigration.java 拆除 channels() 的「config 列未就绪」DataAccessException 容错（V5 已保证列存在，时序泥潭消除）
- [X] T010 升级类旧单测改写为 migration 测试：oryxos-storage/src/test/java/io/oryxos/storage/migration/ 下按原 fixture 手法（裸 DriverManager 造旧库）改写 AuditSchemaUpgradeTest→V2 测试、MemorySchemaUpgradeTest→V3、ScheduleSchemaUpgradeTest（含 IntegrationTest）→V4、并新增 V5 测试；断言收敛结构与升级类时代逐列一致
- [X] T011 Checkpoint：`mvn -q spotless:apply && mvn install` 全绿（此刻 SQLite 档全量测试即须全过——零破坏在骨架期就守住）

## Phase 3: User Story 2 - 单机用户零配置无感升级（P1）

**Goal**: 存量工作区零配置升级无损；新空库零配置可用（FR-002/004、SC-001/006）
**Independent Test**: 旧版本产生带数据工作区 → 新 jar 零配置启动 → 数据完好功能正常

- [X] T012 [US2] 新增存量接管集成测试 oryxos-boot/src/test/java/io/oryxos/boot/LegacyTakeoverIT.java（SpringApplicationBuilder 手工启停范式，参照 ScheduleSchemaUpgradeIntegrationTest）：fixture 造「缺 trace_id/agent_name/config 列 + legacy schedule 键」的旧库 → 启动 → 断言 flyway_schema_history 有 baseline(0)+V1~V5、结构收敛、存量行数据完好；二次重启断言 history 无新增
- [X] T013 [P] [US2] storage 测试基座去重：新建 oryxos-storage/src/test/java/io/oryxos/storage/SqliteJpaTest.java 组合注解（@DataJpaTest + Replace.NONE + 四件套属性），8 个 @DataJpaTest 类（LlmCall/ToolInvocation/Session×2/MemoryEntry/Secret/WebUser/WebSession）迁移到基座（sql.init 属性换成 flyway 属性）
- [X] T014 [US2] 真机走查 quickstart V1+V2：旧 jar 造数据 → 新 jar 零配置升级 → sqlite3 断言 history 与数据；grep 确认升级类零残留；结果记入走查笔记（供 acceptance-report）

**Checkpoint**: US2 独立可验收——SQLite 用户面完整无损

## Phase 4: User Story 1 - 企业管理员用共享数据库部署（P1）

**Goal**: 配 PG url+凭证即全量落库，多进程共同事实源，故障报错可定位（FR-001/006/008/009/010/011、SC-002/003/007/008）
**Independent Test**: 配置 PG 启动 → 对话数据库侧可查 → 第二进程可见同一数据

- [X] T015 [P] [US1] 新建 oryxos-storage/src/main/resources/db/migration/postgresql/V1__baseline.sql：完整最终结构（= SQLite V1~V5 收敛等价，含全部后加列与索引），按 data-model 转写规则——14 处 AUTOINCREMENT→`BIGINT GENERATED BY DEFAULT AS IDENTITY`、7 处 BOOLEAN DEFAULT 1/0→TRUE/FALSE、2 处 BLOB→BYTEA、REAL→DOUBLE PRECISION
- [X] T016 [P] [US1] 新建 oryxos-storage/src/main/java/io/oryxos/storage/config/DataSourceVendorPostProcessor.java（EnvironmentPostProcessor）+ META-INF/spring.factories 注册：url 为 jdbc:postgresql 前缀时清除 journal_mode/busy_timeout 两个 hikari data-source-properties 与 database-platform=SQLiteDialect 默认；sqlite/缺省零动作
- [X] T017 [US1] CLI 轻命令 url 收口：oryxos-cli/.../command/KnowledgeCommand.java、SessionListCommand.java、StatusCommand.java——url 从外部配置合并链解析（复用/抽取配置读取小工具），`Files.exists(oryxos.db)` 检查仅 sqlite url 生效，PG url 走连接探测；SQL 语句不变（已标准语法）
- [X] T018 [US1] 新建 PG 测试基座 oryxos-storage/src/test/java/io/oryxos/storage/PostgresJpaTest.java（zonky embedded-postgres 每套件一实例 + 随机端口）；将全部 8 个 @DataJpaTest 类（LlmCall/ToolInvocation/Session/SessionManager/MemoryEntry/Secret/WebUser/WebSession）的用例抽象为共享基类，Sqlite/Postgres 两 vendor 子类各自实跑——同一套用例两库全绿（SC-005），含向量 BYTEA 读写与 enc:v1: 密文往返断言（FR-009）
- [X] T019 [US1] 新建 oryxos-boot/src/test/java/io/oryxos/boot/PostgresStorageE2ETest.java：zonky 起 PG → mock provider 全链路（对话+工具调用→审计三表 trace 同链→save/recall 记忆→重启第二上下文连同库断言数据可见——SC-002/003 的测试面等价物）
- [X] T020 [P] [US1] 故障报错三分类测试（可并入 T019 类）：不可达 url / 错误凭证 / 只读账号 → 启动失败且报错含可定位信息（SC-008）；断言不静默降级
- [X] T021 [P] [US1] config/application.yml.example 补 PG 示例段（注释态：url/username/`${ORYXOS_DB_PASSWORD}` 占位 + 「库类型按 url 自动识别」说明）；docker/docker-compose.yml 的「SQLite ⇒ 只能单副本」注释补 PG 选项口径
- [X] T022 [US1] 真机走查 quickstart V3+V5：zonky 或真 PG 实例——两进程共同事实源、psql 侧数据断言、三类故障报错；30 并发独立会话写入零锁冲突（SC-007，对照 SQLite 档）

**Checkpoint**: US1 独立可验收——PG 部署选项完整

## Phase 5: User Story 3 - 表结构变更一次定义两库生效（P2）

**Goal**: 迁移体系可演进：加迁移自动生效恰好一次，多副本并发启动安全（FR-003/005、SC-004）
**Independent Test**: 加测试迁移 → 两库自动应用且重启不重复；双进程并发启动迁移恰一次

- [X] T023 [US3] 并发启动测试：oryxos-boot/src/test/java/io/oryxos/boot/ConcurrentMigrationIT.java——空 PG（zonky）双上下文并发启动，断言 flyway_schema_history 每版本恰一条成功记录、双方均正常就绪（SC-004）；SQLite 档明确为单副本档（compose 注释口径），不承诺并发启动
- [X] T024 [P] [US3] 演进流程测试：测试性 V900 迁移脚本（两 vendor 目录各一份）经测试 classpath 注入 → 断言自动应用、重启幂等、失败迁移（故意坏 SQL）拒启且报错含版本号（FR-011）；另断言中断恢复——模拟迁移半完成状态（列已加但 history 未记成功，即执行中途被杀的落盘形态）→ 重启幂等序列收敛成功（Edge case：迁移执行到一半进程被杀）
- [X] T025 [P] [US3] 开发者迁移指南：docs/TechnicalSolution.md 存储节（或 CLAUDE.md 陷阱表）写明「加列三步」——两 vendor 目录同版本号各一份、只增不改、前向兼容约束（contracts §2.5 口径）；「hibernate ddl-auto 迁移」陷阱行更新为 Flyway 口径

**Checkpoint**: 全部故事可独立验收

## Phase 6: Polish & 收尾

- [X] T026 [P] 文档同步（R8）：.specify/memory/constitution.md §技术栈持久化行 PATCH 修订（含 Sync Impact Report 头注更新）；CLAUDE.md 技术栈表与「常见陷阱」表；docs/CliGuide.md 部署配置节补 PG
- [X] T027 [P] SqliteDataSourceConfigTest 确认仅 sqlite 路径断言 PRAGMA（PG 下不适用则条件化）；全仓 grep `sql.init|SchemaUpgrade|schema.sql` 残留清零
- [X] T028 `mvn -q spotless:apply && mvn verify` 全量门禁全绿（含 OWASP 对 flyway/postgresql/zonky 新增面；如报 CVE 按门禁纪律处置）；两库测试零跳过零豁免（SC-005）
- [X] T029 quickstart V1~V6 完整走查落卷 specs/025-pluggable-storage/acceptance-report.md（SC-001~008 对照表）

## Dependencies

- Phase 1 → Phase 2 → US2 → US1 → US3 → Polish（US1 依赖 Foundational 而非 US2，但 US2 先行守零破坏；US3 的并发/演进测试依赖 US1 的 PG 基座 T018）
- T003 阻塞 T004~T007（V1 先定基线）；T008 依赖 T004~T007；T009 依赖 T008；T010 依赖 T009
- T018 阻塞 T019/T023

## Parallel Example

- Phase 2：T004~T007 四个 Java migration 平移可并行（互不同文件）
- US1：T015（PG DDL）/ T016（PostProcessor）/ T021（模板）三线并行；T020 可与 T019 并行
- Polish：T026/T027 并行

## Implementation Strategy

MVP = Phase 1+2+US2（SQLite 用户面零破坏的 Flyway 收编——即使 PG 面延后，收编本身已还清演进债）；随后 US1 交付 PG 部署选项，US3 补演进与并发保障。T011/T014/T022 三个 checkpoint 各自可停可验。
