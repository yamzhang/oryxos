<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.oryxos</groupId>
        <artifactId>oryxos</artifactId>
        <version>0.1.5-RELEASE</version>
    </parent>

    <artifactId>oryxos-web</artifactId>
    <name>OryxOS Web</name>
    <description>Web service: REST API controllers, GlobalExceptionHandler, OpenAPI</description>

    <properties>
        <!-- 前端构建默认随 mvn 触发；快速纯 Java 构建可 -Dfrontend.skip=true 跳过 -->
        <frontend.skip>false</frontend.skip>
        <frontend.node.version>v20.18.0</frontend.node.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>io.oryxos</groupId>
            <artifactId>oryxos-core</artifactId>
        </dependency>
        <!-- 025-persona-library: PersonaApiController 调 PersonaService（人格库合并列表 + 自定义 CRUD）；web→persona 单向 -->
        <dependency>
            <groupId>io.oryxos</groupId>
            <artifactId>oryxos-persona</artifactId>
        </dependency>
        <!-- 012-web-auth: BasicAuthFilter / AuthStartupCheck 调 WebUserService（账号校验）；web→storage 单向，无循环 -->
        <dependency>
            <groupId>io.oryxos</groupId>
            <artifactId>oryxos-storage</artifactId>
        </dependency>
        <!-- 012-web-auth fix: ProviderStartupCheck 调 ProvidersProperties.validate()（仅 serve/gateway 校验 api-key） -->
        <dependency>
            <groupId>io.oryxos</groupId>
            <artifactId>oryxos-provider</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <!-- 012-web-auth: 声明 PasswordEncoder bean（DelegatingPasswordEncoder，{bcrypt} 前缀）；单 jar，非 starter-security 全套 -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-crypto</artifactId>
        </dependency>
        <!-- 040 OIDC：JWKS / id_token 验签（非 spring-boot-starter-security） -->
        <dependency>
            <groupId>com.nimbusds</groupId>
            <artifactId>nimbus-jose-jwt</artifactId>
            <version>9.40</version>
        </dependency>
        <!-- OpenAPI / Swagger UI -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        </dependency>
        <dependency>
            <groupId>com.github.spotbugs</groupId>
            <artifactId>spotbugs-annotations</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- 知识库端点测试用真实本地后端（内存存储档），只进测试路径 -->
        <dependency>
            <groupId>io.oryxos</groupId>
            <artifactId>oryxos-knowledge</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- 测试复用 core 的 SymlinkAssumptions 环境探针（软链/POSIX 用例在 Windows 无权限时优雅 skip） -->
        <dependency>
            <groupId>io.oryxos</groupId>
            <artifactId>oryxos-core</artifactId>
            <version>${project.version}</version>
            <type>test-jar</type>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- 管理台 Vue 前端：npm install + vite build，产物落 src/main/resources/static/admin，
                 随 process-resources 进 fat JAR、由 Spring 托在 /admin。绑在 generate-resources
                 （早于 process-resources / test），一条 mvn 命令出全量包。 -->
            <plugin>
                <groupId>com.github.eirslett</groupId>
                <artifactId>frontend-maven-plugin</artifactId>
                <version>1.15.1</version>
                <configuration>
                    <workingDirectory>src/main/frontend</workingDirectory>
                    <installDirectory>target</installDirectory>
                    <skip>${frontend.skip}</skip>
                </configuration>
                <executions>
                    <execution>
                        <id>install-node-and-npm</id>
                        <goals><goal>install-node-and-npm</goal></goals>
                        <phase>generate-resources</phase>
                        <configuration>
                            <nodeVersion>${frontend.node.version}</nodeVersion>
                        </configuration>
                    </execution>
                    <execution>
                        <id>npm-install</id>
                        <goals><goal>npm</goal></goals>
                        <phase>generate-resources</phase>
                        <configuration>
                            <arguments>install</arguments>
                        </configuration>
                    </execution>
                    <execution>
                        <id>npm-test</id>
                        <goals><goal>npm</goal></goals>
                        <phase>generate-resources</phase>
                        <configuration>
                            <arguments>test</arguments>
                        </configuration>
                    </execution>
                    <execution>
                        <id>npm-build</id>
                        <goals><goal>npm</goal></goals>
                        <phase>generate-resources</phase>
                        <configuration>
                            <arguments>run build</arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
