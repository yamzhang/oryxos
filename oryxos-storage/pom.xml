<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.oryxos</groupId>
        <artifactId>oryxos</artifactId>
        <version>0.1.5-RELEASE</version>
    </parent>

    <artifactId>oryxos-storage</artifactId>
    <name>OryxOS Storage</name>
    <description>Persistence: SQLite, SessionRepository, ToolInvocationRepository, LlmCallRepository</description>

    <dependencies>
        <dependency>
            <groupId>io.oryxos</groupId>
            <artifactId>oryxos-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.hibernate.orm</groupId>
            <artifactId>hibernate-community-dialects</artifactId>
        </dependency>
        <!-- 025-pluggable-storage: 表结构唯一真相源 db/migration/{vendor}（宪法 VIII 预留的 Flyway 口）。
             SQLite 支持内置于 flyway-core（flyway-database-sqlite 构件不存在，2026-09 实证）；
             PG 支持自 Flyway 10 拆分为独立插件。三者均打进 fat jar：PG 是部署选项，即开即用不换包。 -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
        </dependency>
        <!-- 嵌入式 PG：本机与 CI 同构的「同套用例两库全绿」载体（research R5，docker 不可用环境唯一可行路径） -->
        <dependency>
            <groupId>io.zonky.test</groupId>
            <artifactId>embedded-postgres</artifactId>
            <version>2.0.7</version>
            <scope>test</scope>
        </dependency>
        <!-- 012-web-auth: 密码哈希用 DelegatingPasswordEncoder（{bcrypt} 前缀），单 jar，非 spring-boot-starter-security 全套 -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-crypto</artifactId>
        </dependency>
        <!-- 012-web-auth US3: @SuppressFBWarnings 注解（provided 不进运行时，跟 oryxos-web 一致） -->
        <dependency>
            <groupId>com.github.spotbugs</groupId>
            <artifactId>spotbugs-annotations</artifactId>
            <scope>provided</scope>
        </dependency>
    </dependencies>

</project>
